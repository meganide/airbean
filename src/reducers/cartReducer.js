const INITAL_STATE = [{}];

function cartReducer(state = INITAL_STATE, action) {
  switch (action.type) {
    case value:
      break;

    default:
      return state;
  }
}

export { cartReducer };
